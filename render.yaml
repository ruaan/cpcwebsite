services:
  - type: web
    name: crest-point-capital
    env: static
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    staticPublishPath: .
